# This file is auto-generated by oagen. Do not edit.

require "json"

module WorkOS
  class DirectoryUserWithGroups
    include HashProvider

    # @!attribute emails
    #   @deprecated A list of email addresses for the user.
    # @!attribute job_title
    #   @deprecated The job title of the user.
    # @!attribute username
    #   @deprecated The username of the user.
    # @!attribute raw_attributes
    #   @deprecated The raw attributes received from the directory provider.

    attr_accessor \
      :object,
      :id,
      :directory_id,
      :organization_id,
      :idp_id,
      :email,
      :first_name,
      :last_name,
      :emails,
      :job_title,
      :username,
      :state,
      :raw_attributes,
      :custom_attributes,
      :role,
      :roles,
      :created_at,
      :updated_at,
      :groups

    def initialize(json)
      hash = json.is_a?(Hash) ? json : JSON.parse(json, symbolize_names: true)
      hash = hash.transform_keys(&:to_sym) if hash.keys.first.is_a?(String)
      @object = hash[:object]
      @id = hash[:id]
      @directory_id = hash[:directory_id]
      @organization_id = hash[:organization_id]
      @idp_id = hash[:idp_id]
      @email = hash[:email]
      @first_name = hash[:first_name]
      @last_name = hash[:last_name]
      @emails = (hash[:emails] || []).map { |item| item ? WorkOS::DirectoryUserWithGroupsEmail.new(item.to_json) : nil }
      @job_title = hash[:job_title]
      @username = hash[:username]
      @state = hash[:state]
      @raw_attributes = hash[:raw_attributes] || {}
      @custom_attributes = hash[:custom_attributes] || {}
      @role = hash[:role] ? WorkOS::SlimRole.new(hash[:role].to_json) : nil
      @roles = (hash[:roles] || []).map { |item| item ? WorkOS::SlimRole.new(item.to_json) : nil }
      @created_at = hash[:created_at]
      @updated_at = hash[:updated_at]
      @groups = (hash[:groups] || []).map { |item| item ? WorkOS::DirectoryGroup.new(item.to_json) : nil }
    end

    def to_json(*)
      {
        object: object,
        id: id,
        directory_id: directory_id,
        organization_id: organization_id,
        idp_id: idp_id,
        email: email,
        first_name: first_name,
        last_name: last_name,
        emails: (emails || []).map(&:to_json),
        job_title: job_title,
        username: username,
        state: state,
        raw_attributes: raw_attributes,
        custom_attributes: custom_attributes,
        role: role&.to_json,
        roles: (roles || []).map(&:to_json),
        created_at: created_at,
        updated_at: updated_at,
        groups: (groups || []).map(&:to_json)
      }
    end
  end
end
